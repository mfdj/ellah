{
   "name": "ellah",
   "version": "0.0.1",
   "description": "Development environment helper",
   "install": "install -b ellah.sh ${PREFIX:-/usr/local}/bin/ellah",
   "scripts": [ "bin/ellah" ],
   "dependencies": {
      "mfdj/colored-logger.bash": "0.0.3"
   }
}
